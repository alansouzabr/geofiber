export default function EmpresasPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Empresas</h1>
      <p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">
        Aqui vamos encaixar o módulo de cadastro e seleção de empresas.
      </p>
    </div>
  );
}
